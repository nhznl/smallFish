# stock-app — pinned dependencies
#
# Pins match exactly what the venv installed (pip freeze), so the FastAPI
# backend reproduces byte-for-byte. This runtime is separate from utilities/
# and studies/; both Python runtimes may import services/ for provider transport
# only — never the other way around.
#
# pandas, numpy, PyYAML, yfinance, and requests remain here because the API
# reads shared artifacts and subprocesses batch jobs that use those libraries.
# tastytrade and snaptrade-python-sdk back optional brokerage integrations.
# The rest are the API + test stack.

# --- shared artifact / batch-job deps (also in utilities/) ---
pandas==3.0.0
numpy==2.4.2
PyYAML==6.0.3
yfinance==1.1.0
requests==2.32.5
tastytrade==13.2.0
snaptrade-python-sdk==12.0.0

# --- API stack ---
fastapi==0.139.2
uvicorn[standard]==0.51.0

httpx==0.28.1

# --- tests ---
pytest==9.1.1
