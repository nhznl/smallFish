# stock-app — pinned dependencies
#
# Pins match exactly what the venv installed (pip freeze), so the FastAPI
# backend reproduces byte-for-byte. Duplicated strategy code needs pandas /
# numpy / PyYAML / yfinance / requests at the versions strategy/ uses; the
# rest are the API + diff-harness + test stack.

# --- duplicated-strategy-code deps (match strategy/) ---
pandas==3.0.0
numpy==2.4.2
PyYAML==6.0.3
yfinance==1.1.0
requests==2.32.5
tastytrade==13.2.0
snaptrade-python-sdk==12.0.0

# --- API stack ---
fastapi==0.139.2
uvicorn[standard]==0.51.0

httpx==0.28.1

# --- tests ---
pytest==9.1.1
